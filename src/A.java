public class A {
    // classifiers private, package private, protected, public - модификаторы доступа
    // final, static - статик - обращение к классу, а не объекту; финал - не изменяемый объект,
    // если применить финал к методу, то мы не сможем сделать оверрайд.
    // в родителей можно кастовать детей, но не наоборот
    // instanceof может определить можно ли в один объект положить другой (определяет по принадлежности
    // к классу).
}
